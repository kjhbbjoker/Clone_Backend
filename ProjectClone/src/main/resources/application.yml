spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/projectboard?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: hjk1234


  jpa:
    hibernate:
      ddl-auto: create #none , update, validate
    properties:
      hibernate:
        format_sql: true

  jackson:
    property-naming-strategy: SNAKE_CASE

logging.level:
  org.hibernate.SQL: debug
  org.apache.coyote.http11: debug